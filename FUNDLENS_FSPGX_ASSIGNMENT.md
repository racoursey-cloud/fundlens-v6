FABIO'S FSPGX PIPELINE-REPAIR WAVE ORDER (ratified by Robert, July 28, 2026)
Scope: ONE pull request off main at 0b3bb49. Five one-file commits, in the order below. Two server files (src/engine/edgar.ts, src/engine/persist.ts), one client file, two documents. No SQL, no schema changes, and no database writes of any kind during build or verification — the dry-run below is read-only against EDGAR. No v8-protected files (regime*, race*, race-boot*, french*, contenders/). types.ts is NOT expected to change; if a change there proves necessary, STOP and report the exact diff for Fabio's v8-section review before committing. Never touch holdings_cache rows by hand: FSPGX's July-24 rows are its only in-cache copy until this wave lands.
Ruled facts this wave executes:

* Architecture (Robert, July 28): per-series EDGAR lookup becomes the primary path. The existing candidate scan stays in the file, byte-unchanged, as the fallback. The 60 cap is not raised.
* Donut (Robert, July 28): the reference Sectors legend ×100 fix rides here as its own commit.
* Diagnosis of record: FSPGX is series S000054095 under CIK 35315. Its newest NPORT-P is accession 0000035402-26-004115, filed June 26, period April 30, 2026, sitting at candidate position 64 of 599 in the trust feed — past the cap at edgar.ts line 460. The nightly gate failures since July 25 follow from that arithmetic. Its cohort files quarterly; without this fix the fund stays dark until roughly September 25.

MID-WAVE EVIDENCE GATE — after c0 and c1, before c2 or c3: read how result.fundDetails is constructed in src/engine/pipeline.ts and report whether each detail object carries the filing metadata (EdgarFilingResult.meta, holding reportDate and accessionNumber). If yes, c3 threads from detail.meta. If no, c3 threads from the dossierByTicker map already built at the top of persistPipelineResults — dossiers verifiably carry d.reportDate and d.accessionNumber. Report which source you will use, with the line numbers you read, then WAIT for Fabio's confirm relayed by Robert. An unanswered question is never a decline; if the relay is interrupted, re-ask.
THE COMMITS
c0 — the B4-build closing handoff you hold under the stand-down order. Commit it to the repo root under its held filename, byte-for-byte as held. Before committing, count in your held copy the occurrences of the two-character sequence quoted in the next sentence, and state that number in your report. Acceptance: grep -F for '](' on the committed file returns exactly the number you declared, on the same lines. The held text is never reworded, even to satisfy a check.
c1 — FUNDLENS_FSPGX_ASSIGNMENT.md at the repo root: the full text of this order, exactly as received between the BEGIN ORDER and END ORDER marker lines, markers excluded. This order deliberately contains no web addresses and no markdown links; the only occurrence of the grep sequence in it is the single quotation inside the c0 acceptance sentence. Acceptance: the committed file contains exactly ONE match for that sequence, located in that sentence — zero or two-plus is a FAIL. If any line arrives mangled into link syntax, reconstruct it from structure; the ratified text is never reworded.
c2 — src/engine/edgar.ts. Inside findLatestNportFiling only, add a series-direct resolution step between candidate collection and the peek loop, active only when targetSeriesId is present:

1. Build a request to the browse-edgar CGI on the same host checkFilingSeriesId already fetches from — derive the host from the Archives URL that function builds near line 500; hard-code no new host. Path: /cgi-bin/browse-edgar. Query parameters: action set to getcompany; CIK set to targetSeriesId (the S-number is accepted directly); type set to NPORT-P; count set to 10; output set to atom. Send the existing EDGAR.USER_AGENT header, with the standard PIPELINE.API_CALL_DELAY_MS delay before the request — sequential like every other EDGAR call, never parallel, no Promise.all.
2. From the atom response, extract the accession-number values in document order (newest first). Plain regex extraction is fine; add no dependencies.
3. Take the first accession and look it up in the candidates array already built from the submissions feed — search the whole array, no cap applies to a string match. On a hit, run checkFilingSeriesId once on that single candidate as confirmation, then return it. Log the path, naming the series and accession, so nightly logs show series-direct matches.
4. Fallback: if the atom request fails, returns no entries, the accession is absent from candidates, or the confirmation peek does not match, log a warning and fall through to the existing peek loop exactly as it stands — cap, Salem Street comment, and all.
5. Update the final no-match warning so a total failure names both paths, series-direct and scan.
Acceptance greps on the committed file: it contains the fragments browse-edgar and output=atom; the slice cap line and its comment are byte-unchanged; the diff contains no Promise.all.

c3 — src/engine/persist.ts. Three changes, one commit:

1. Real stamps: in the mappedRows literal, replace the empty accession_number and the fetch-day report_date with the fund's real filing values from the source confirmed at the mid-wave gate. One accession and one report date per fund, applied to all its rows. If — and only if — neither source carries values for a fund that nonetheless produced rows, keep today's fetch-day stamp for that fund and log a loud warning naming the ticker; never invent a date.
2. Insert first, then scoped delete: remove the pre-insert blanket delete. Insert the chunked rows exactly as today, upsert still true — with real accessions, the unique index on fund_id, accession_number, cusip makes re-runs idempotent. Only after ALL chunks for the fund succeed, delete that fund's rows whose accession_number differs from the just-written accession. That purges older filings and every legacy blank-accession row, including FSPGX's July-24 group, automatically on each fund's first good fetch. If any chunk fails, skip the delete and log it: the fund keeps serving its last good filing instead of going empty. The July-1 zero-row window ceases to exist by construction.
3. Sweep the stale comment near line 215 so it states the true unique index (fund_id, accession_number, cusip) and the new insert-then-scoped-delete order.
Acceptance: the committed file contains no unconditional whole-fund holdings delete before insert; the dossier and scores sections are untouched.

c4 — client/src/pages/reference/FundDetail.tsx. In buildSectorSlices, stop multiplying the payload's sector value by 100 — the stored map is already on the 0-to-100 scale (FXAIX's stored map sums to about 100; DonutChart's own contract comment says 0–100). Pass the value through. Zero other logic changes, zero copy changes, and no edit to DonutChart itself — that is a full-tier file, read-only.
VERIFY AT HEAD, THEN STOP

* Fresh-container order: npm ci at the repo root first, then tsc with noEmit, then npm run build from the repo root — root install alone does not cover client deps; client has its own package.json.
* Dry-run proof, database-write-free: a scratch script, NOT committed, path named in your report, importing nothing from supabase, calling fetchEdgarHoldings sequentially with the standard delay for FSPGX and for one healthy multi-series control fund you name from the 23-fund roster. Required: FSPGX returns success true, meta accession 0000035402-26-004115, holdings count near 392, via the series-direct log line; the control fund resolves to the SAME accession through series-direct as through the fallback scan — run both paths for the control inside the scratch script and print both.
* Report: each commit sha; the c0 declared count and the committed-file grep result; the c1 match count and its line number; the c2 and c3 acceptance grep outputs; tsc and build outputs; the dry-run log lines; the PR head sha. Then STOP. Fabio's independent pre-merge review follows at the head; Robert merges with "Create a merge commit" — never squash, never rebase — and Robert's morning-after check is: no FSPGX alert email, and the FSPGX detail page serving holdings with provenance dated 2026-04-30.

AMENDMENTS AT GATE — July 29, 2026 (relayed by Robert; cures and confirms by Fabio)
A1 — c0 RE-SUPPLIED. The held-copy presumption was false: no B4-build closing handoff was ever delivered to Clyde, and no authoring order was ever issued — the stand-down order presumed a document that did not exist. Cured by re-supply per the relay law, no fault hunted. Fabio authored the document and delivered it in the same relay as this amendment. Filename: FUNDLENS_FSPGX_HANDOFF.md at the repo root. Its content contains no web addresses, no markdown links, and zero occurrences of the two-character sequence quoted in the original c0 acceptance sentence above. c0's acceptance is now: the committed file is byte-identical to the supplied text; its first line begins "# FundLens — B4 Build"; its last line ends "— Fabio, July 28, 2026"; and a grep for that quoted sequence returns nothing.
A2 — c1 BOUNDARIES CONFIRMED. The order's marker instruction was defective — Robert was told to exclude the marker lines while c1 defined its content by them. The received text is authoritative: the order runs exactly from the line beginning "FABIO'S FSPGX PIPELINE-REPAIR WAVE ORDER" through the line ending "provenance dated 2026-04-30." This AMENDMENTS AT GATE section is appended beneath that final line, separated by one blank line. The amendment adds no web addresses, no markdown links, and no new occurrence of the quoted sequence, so the committed assignment contains exactly ONE match, in the original c0 acceptance sentence.
A3 — MID-WAVE GATE CONFIRMED. fundDetails carries no filing metadata: FundPipelineDetail is six fields exactly, its construction sets only those six, and no filing metadata reaches the map — Clyde's citations, independently re-verified by Fabio. c3 therefore threads the real accession number and report date from the dossierByTicker map already in scope in persistPipelineResults, one accession and one report date per fund, applied to all its rows. The null-dossier fallback — fetch-day stamp with a loud warning naming the ticker — stands as a tripwire expected never to fire, because a fund whose fetch fails produces no rows. With this gate answered, c2 and c3 are cleared once c0 and c1 land.
